# Vercel Environment Variables Template
# Copy these to your Vercel project dashboard under Settings > Environment Variables

# SMTP Configuration (Required)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Email Configuration
MAIL_FROM="RapidAuth.pro <noreply@rapidauth.pro>"

# JWT Secret (Required - Generate a strong secret)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Redis Configuration (Optional - for better OTP storage)
REDIS_URL=redis://username:password@hostname:port

# License Configuration (Optional)
LICENSE=your-license-key
LICENSE_KEYS=valid-key-1,valid-key-2
LICENSE_SERVER=https://your-license-server.com

# Node Environment
NODE_ENV=production
